apiVersion: v1
kind: PersistentVolume
metadata:
  name: input-data-pv
  labels:
    name: input-data-pv
    type: local
spec:
  storageClassName: manual
  capacity:
    storage: 16Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: {{ .Values.localVolumeInputDataPath }}